# 顔アイコンの置き場所（任意）

**通常は空のままで構いません。** 顔アイコンは `../` に置いた立ち絵から自動で切り抜かれます。

自動切り抜きの結果ではなく、自分で用意した顔アイコンを使いたい場合だけ、
立ち絵と同じファイル名（例: `ch_rizel.png`）でここに置いてください。
ここにファイルがあれば、自動切り抜きより優先して使われます。

推奨サイズは正方形（512×512 など）です。

`test/art.html` の「顔アイコンを書き出す」で書き出したPNGを、そのままここに置くこともできます。

# 灰銀の継承者 — 作業のきまり

このファイルは**毎回読まれる**。だから短くしてある。
「読めば分かること」は書かない。**知らずに触ると壊すこと**だけを書く。

- ゲームデザインの理念・数式 … `../game_design_prompt.md`
- 世界観 … `docs/灰銀の継承者 世界設定素案2.md`
- 拡張コンテンツの書き方 … `docs/拡張コンテンツの作り方.md`
- 効果を足す手順 … `docs/効果を追加するときの手引き.md`
- 今は着手しないと決めたもの … `docs/保留中のアイデア.md`

---

## 1. まず知るべき構造

**素の `<script>` とグローバル `RPG` 名前空間で動いている。** ESモジュールは使えない
（`file://` で開けることを要件にしているため）。`import` / `export` を書くと動かない。

型は `// @ts-check` と JSDoc で見ている。`jsconfig.json` がそれを拾う。

```
data/      内容（キャラ・敵・技・装備・フィールド・ツリー・物語）。ここの追記だけで増える
src/core/  規則（damage / battle / tree / units / state / worldmap / story …）
src/ui/    見せ方。規則を持たない
src/plugins/ 技1つぶんの振る舞い
test/      index.html=検証テスト / balance.html=バランス実測 / builds.html=ビルド確認
tools/     公開・画像・取り込みのスクリプト
```

**データ駆動を崩さないこと。** キャラ・敵・技・装備・フィールドの追加で
`src/` を触る必要が出たら、それは設計が壊れかけている合図。

---

## 2. 効果を1つ足すときに触る場所

一番よく壊れるところ。**1箇所でも漏らすと、書いたのに何も起きない。**
エラーも警告も出ない。

| ファイル | 何を書くか |
|---|---|
| `data/effectkinds.js` | 登録簿。`to` / `key` / `label` / `fmt` |
| `src/core/tree.js` | ① passives/situational の既定値 ② `switch` の `case` ③ `KNOWN_EFFECT_KINDS` |
| `data/nodecategories.js` | ビルド画面のどの枝に並ぶか |
| `docs/拡張コンテンツの作り方.md` | 外部向けの表 |
| `test/tests.js` | ① §5 の検査一覧 ② ユニーク効果の `PROBES` |

**5ファイル7箇所。** 検証テストがこの全部を突き合わせているので、
漏らせば必ず落ちる。落ちたら「テストが厳しい」ではなく**登録漏れ**だと思うこと。

### 効果の届き先を間違えない

3つの経路があり、**取り違えても静かに無効になる**。

- `passives.*` — 戦闘中に `battle.js` が読む
- `situational.*` — `units.toAttacker` が拾い、`damage.js` が読む
- 素の値 — ユニット直付け

`data/effectkinds.js` の `to` がその宣言。迷ったら似た効果の行を真似る。

---

## 3. 数値を触ったら必ず測り直す

**感覚で決めない。** `test/balance.html` で実測してから決める。
`balance-tuning` スキルに手順がある。

測るときの注意:

- **上乗せではなく置き換えで測る。** 支援パッシブを攻撃ビルドに足すと、枠の対価を
  払っていないので過大評価になる。パーティの1枠を実際に差し替えて比べる
- **基準は `maxLevelCap`（255）。** `maxLevel`（150）で測ると、上限を伸ばした先で破綻する
- **勝率よりラウンド数のほうが安定する。** 20戦程度では勝率は数ポイント揺れる
- 1回に1つだけ変える。2つ動かすと、どちらが効いたか分からなくなる

---

## 4. 公開の手順

`hakusura-release` スキルに全部書いてある。飛ばさないこと。

```
python tools/publish_check.py       公開して困るものが混ざっていないか
python tools/build_precache.py      ファイルを増やしたときだけ
python tools/bump_cache_version.py  必ず。上げないと端末に届かない
git commit / push
公開先の sw.js を叩いて CACHE_VERSION が上がったか確認
```

`bump_cache_version.py` は**プリキャッシュ一覧を作り直さない**。
ファイルを増やしたら `build_precache.py` を先に回す。

拠点にいるときは更新が自動で当たる（`index.html` の更新機構）。
戦闘中とシーン再生中だけは知らせるに留めている。

---

## 5. 触ってはいけないもの

- **`raw_image/` の原本**。加工は別ディレクトリへ出す
- **画像生成は指示があるまで走らせない**。課金される
- 生成リクエストは**逐次＋待ち時間**を守る。提供元へ負荷をかけない
- APIキーは会話に出さない。`NOVELAI_API_TOKEN` にしか置かない
- テストページは `localStorage` を**退避して復元する**。実データを壊さない

---

## 6. セーブの互換性

新しいキーは**新規セーブの初期形にも置く**。移行処理だけで補うと、
「新規作成した形」と「読み込んだ形」がずれて、書き出し→読み込みの往復が一致しなくなる。
`state.js` の `arena` と `presetSlots` にその理由がコメントで残っている。

ストーリー側は別プロファイル。`PROFILE_KEYS` に入れたキーは
`state.js` の Proxy がモードで切り替える。**周回側へ漏らさない。**

---

## 7. 直すときの作法

- **落ちたテストを消して通さない。** テストが古いなら、なぜ古いのかを直す
- **原因を測ってから直す。** 今日の不具合の多くは「そう見えた」ものと違う場所にあった
- コメントは**なぜそうしたか**を書く。何をしているかはコードが言う
- 実装と食い違う説明は、説明が無いより悪い。数値を文章に書いたら
  テストで実装の定数と突き合わせる（`data/glossary.js` がその形）
